# Reflection

This project helped me understand how to decouple UI from logic using schema-driven design. Instead of hardcoding forms, the system becomes flexible and reusable.

The most challenging part was implementing conditional rendering and validation in a generic way.

If extended further, I would:

* Add nested sections
* Support async validation
* Optimize rendering with memoization
